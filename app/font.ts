import { Inter, Pangolin } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const pangolin = Pangolin({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
